# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index
POST    /characters/create          controllers.Application.createOrUpdateCharacter
GET     /characters/:id             controllers.Application.findCharacterById(id: Long)
GET     /characters/:name           controllers.Application.findCharacterByName(name: String)
GET     /characters                 controllers.Application.findAllCharacters()
DELETE  /characters/:id             controllers.Application.deleteCharacter(id: Long)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
