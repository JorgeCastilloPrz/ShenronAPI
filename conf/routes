# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                          Application.index
POST    /characters/create         Application.createOrUpdateCharacter
POST    /characters/create/        Application.createOrUpdateCharacter
GET     /characters/               Application.findCharacter(id: Option[Long], name: Option[String])
GET     /characters/all            Application.findAllCharacters()
GET     /characters/all/           Application.findAllCharacters()
DELETE  /characters/               Application.deleteCharacter(id: Option[Long])

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file              controllers.Assets.at(path="/public", file)
